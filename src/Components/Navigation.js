import React from 'react';
import '../CSS/Navigation.css'

export default function Navigation(props) {
    return (
        <div id='nav-bar'>
            <h1>just weather</h1>
        </div>
    );
}
